# When I see a bird that walks like aduck and swims like aduck and quackes like a duck, I call that bird a duck. -- Jamse Whitcomb Riley
# The Python Philosophy: Duck Typing
def compute(a, b, c):
    return (a + b) * c

print(compute(4, 1, 3))
print(compute([1], [2, 3], 2))
print(compute('l', 'olo', 4))
